#!/usr/bin/env bash

exit 0
cd "$1"

echo "Detecting Mendix Custom Buildpack"

if [[ -d model && -d web && -f model/metadata.json ]]; then
    echo "Mendix"
    exit 0
else
    echo "no"
    exit 1
fi
